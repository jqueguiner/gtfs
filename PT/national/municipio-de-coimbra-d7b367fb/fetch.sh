#!/usr/bin/env sh
# fetch Município de Coimbra (PT) GTFS -> gtfs.zip. Regenerated by build_repo.py.
URL="https://dados.gov.pt/s/resources/gtfs-estaticos-servicos-municipalizados-de-transportes-urbanos-de-coimbra/20260914-084945-1ed3f73f/smtuc-static-gtfs.zip"
DIR="$(CDPATH= cd "$(dirname "$0")" && pwd)"
echo "GET $URL"
curl -fSL --retry 3 --max-time 300 -o "$DIR/gtfs.zip" "$URL" || { echo "download failed"; exit 1; }
unzip -l "$DIR/gtfs.zip" 2>/dev/null | grep -q stops.txt || { echo "not valid GTFS (no stops.txt)"; exit 2; }
echo "ok: $DIR/gtfs.zip"
