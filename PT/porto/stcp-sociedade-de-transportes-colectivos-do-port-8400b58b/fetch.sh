#!/usr/bin/env sh
# fetch STCP — Sociedade de Transportes Colectivos do Porto (PT) GTFS -> gtfs.zip. Regenerated by build_repo.py.
URL="https://opendata.porto.digital/dataset/5275c986-592c-43f5-8f87-aabbd4e4f3a4/resource/fd65a5ab-baa9-4176-b7b6-27baaeca93bd/download/gtfs_feed.zip"
DIR="$(CDPATH= cd "$(dirname "$0")" && pwd)"
echo "GET $URL"
curl -fSL --retry 3 --max-time 300 -o "$DIR/gtfs.zip" "$URL" || { echo "download failed"; exit 1; }
unzip -l "$DIR/gtfs.zip" 2>/dev/null | grep -q stops.txt || { echo "not valid GTFS (no stops.txt)"; exit 2; }
echo "ok: $DIR/gtfs.zip"
